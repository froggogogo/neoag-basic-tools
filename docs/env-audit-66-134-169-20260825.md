# 三机环境审计报告（66 / 134 / 169）

> 审计时间：2026-08-25 16:50（UTC+8）  
> 脚本：`neoag-basic-tools-install/scripts/audit_host_pipeline_env.sh`  
> 原始 TSV：`/mnt/neoag_100T/majiaxin/neoag-basic-tools-install-deps/manifests/env_audit_20260825/audit_{66,134,169}.tsv`  
> 金标准：134 `/home/na/miniforge3`（sunbinbin 可跑通 LOHHLA/SNAF）

---

## 1. 结论（先看这个）

先前「环境审计/修复」**几乎没用**，因为：

1. **`host_verify.sh` 只查「env 目录是否存在」+ 少量包**（Sequenza `data.table`、SpliceMutr BSgenome、ASCAT、picard），**不检查** LOHHLA 实际使用的 `neoag-fusion` R 包（`optparse` 等），也**不检查** SNAF 的 `mhcgnomes.Class2Pair` / `mhcflurry` 冒烟。
2. 病例侧改脚本、修 `copyNum`、换 HMFTOOLS 路径，属于**运行路径补丁**，补不了 **本机 conda 内容不一致**。
3. LOHHLA 金路径明确走 **`neoag-fusion` 的 Rscript**（`run_lohhla_sample.sh`），66 上另有一个残缺的 `lohhla-mod`（无 `optparse`）——往错 env 装包等于白做。

| 主机 | conda | 相对 134 关键阻塞项 |
|------|-------|---------------------|
| **134** | `/home/na/miniforge3` | 金标准；LOHHLA R 全齐；SNAF Class2Pair OK |
| **66** | `/root/neo/envs/miniforge3` | **`neoag-fusion` 缺 `optparse`/`data.table`** → LOHHLA 必挂；SNAF Class2Pair **现已 OK**，但仍有 `KeyError: query`（mygene/biothings 运行期） |
| **169** | `/root/neo/env_tool/miniforge3` | LOHHLA R 包几乎全缺；`neoag-gatk` **无 picard.jar**；SNAF **`mhcflurry`/`Class2Pair` 坏**（`mhcgnomes` 1.8.6 但 `__init__` 未导出） |

共享层（100T deps：脚本 / HMFTOOLS / refs / predictors / LOHHLA 资产）三机 **一致且可读**；缺口在 **本机 conda 内容**，不是「缺模板脚本」。

---

## 2. 计数摘要

| 主机 | OK | MISSING | FAIL | WARN |
|------|----|---------|------|------|
| 66 | 80 | 4 | 0 | 2 |
| 134 | 79 | 3 | 0 | 1 |
| 169 | 73 | 9 | 1 | 1 |

（ABSENT = 可选 env 不存在，不计入硬失败。）

---

## 3. 工具 × 三机对照（核心）

图例：✅ 可用 · ❌ 不可用 · ⚠ 弱/路径差异 · — 可选

| 模块 | 检查项 | 134 | 66 | 169 | 备注 |
|------|--------|-----|----|-----|------|
| 本机 conda | `NEOAG_CONDA_BASE` | ✅ | ✅ | ✅ | 路径不同（规则允许） |
| HLA | OptiType env | ✅ | ✅ | ✅ | 入口名是 `bin/optitype` 非 `OptiTypePipeline.py`（审计误报路径） |
| HLA | SpecHLA / HLA-LA（100T） | ✅ | ✅ | ✅ | 共享 deps |
| HLA | `spechla_env` | ❌ 路径未挂到 miniforge envs | ✅ | ✅ | 134 可能另有位置；需对齐 site.env |
| CNV | Sequenza + data.table + samtools1.9 | ✅ | ✅ | ✅ | |
| CNV | ASCAT | ✅ | ✅ | ✅ | |
| CNV | FACETS env | ✅ | ✅ | ✅ | `snp-pileup` 在 facets R extcode，非 `bin/`（审计路径过严） |
| CNV | PURPLE / HMFTOOLS（100T） | ✅ | ✅ | ✅ | |
| LOHHLA | 脚本+polysolver+lic（100T） | ✅ | ✅ | ✅ | |
| LOHHLA | `neoag-fusion`:`optparse` | ✅ | ❌ | ❌ | **66/169 现挂点** |
| LOHHLA | `neoag-fusion`:`data.table` | ✅ | ❌ | ❌ | |
| LOHHLA | GenomicRanges/Rsamtools/Biostrings | ✅ | ✅ | ❌ | 169 更残 |
| LOHHLA | picard（neoag-gatk） | ✅ | ✅ | ❌ | 169 缺 jar |
| RNA | STAR/salmon/regtools/VEP/pVAC | ✅ | ✅ | ✅ | |
| SNAF | env + snaf/pandas/mygene | ✅ | ✅ | ✅ | |
| SNAF | `mhcgnomes.Class2Pair` | ✅ | ✅（现） | ❌ | 169 `__init__` 未导出；66 曾坏过 |
| SNAF | `mhcflurry` import | ✅ | ✅ | ❌ | 169 因 Class2Pair 连带失败 |
| SNAF | docker altanalyze | ⚠ 无镜像 | ⚠ | ⚠ | 可走 counts.original.full 回退 |
| SpliceMutr | BSgenome hg38 | ✅ | ✅ | ✅ | |
| 脚本模板 | shared_scripts（100T） | ✅ | ✅ | ✅ | |
| 预测器/refs | licenses + shared_refs | ✅ | ✅ | ✅ | |

---

## 4. 与 jinganxin 失败一一对应

| 现象 | 根因（环境层） | 为何「修过」仍挂 |
|------|----------------|------------------|
| LOHHLA：`there is no package called 'optparse'` | 66 `neoag-fusion` 无 `optparse`（金标准在 134 同名 env 有） | 未把 LOHHLA R 依赖纳入 `ensure_host_runtime` / `host_verify`；`lohhla-mod` 是干扰项 |
| SNAF：曾 `Class2Pair` ImportError | 66 `neoag-snaf` 的 `mhcgnomes` 曾不完整 | 单次 pip 修补未固化进安装验收；**今天冒烟已过** |
| SNAF：现 `KeyError: 'query'` | mygene/biothings 返回列与 snaf 期望不一致（同 pin 下仍可能 API/缓存差异） | 需对照 134 同版本做 **端到端 query 冒烟**，不是改病例脚本 |

---

## 5. 系统性修复方向（不做病例补丁）

按优先级：

1. **以 134 为金标准**，把 `neoag-fusion` LOHHLA 所需 R 包、`neoag-snaf` 的 mhcgnomes/mhcflurry 销钉，写入 `ensure_host_runtime.sh`（conda/mamba 或 conda-pack），在 **66 / 169 重跑主机加固**，禁止手改病例。
2. **扩展 `host_verify.sh` / 本审计脚本**：LOHHLA R 冒烟 + SNAF `Class2Pair` + `mhcflurry` 必过，否则 verify 失败。
3. **169**：另补 `neoag-gatk` picard（conda-pack 或从 134 pack 解包）。
4. **134**：核对 `spechla_env` 真实路径并写进 `site_runtime.sh` 探测列表。
5. 修完后对 jinganxin **重跑 LOHHLA / SNAF**（不改逻辑，只换齐环境）。

---

## 6. 复跑审计

```bash
bash /mnt/neoag_100T/majiaxin/neoag-basic-tools-install-deps/scripts/audit_host_pipeline_env.sh \
  > /mnt/neoag_100T/majiaxin/neoag-basic-tools-install-deps/manifests/env_audit_YYYYMMDD/audit_HOST.tsv
```
